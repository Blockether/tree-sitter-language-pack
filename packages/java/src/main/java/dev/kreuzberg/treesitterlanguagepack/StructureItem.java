// Generated by alef, then EXTENDED BY HAND — do not blindly `alef generate` over it.
// The hand-added component is `kindLabel`: `StructureKind.Other(String)` in Rust carries
// a payload (`{"Other": "resource"}` on the wire) that a Java `enum` cannot hold, so the
// label was silently dropped and every GraphQL `type`, Terraform `resource` and Elixir
// macro collapsed into a bare `Other`. `JsonCodec.structureItem` now lifts that payload
// into `kindLabel` (null for unit variants). Re-apply this component after regeneration.
// To verify freshness: alef verify --exit-code
package dev.kreuzberg.treesitterlanguagepack;

import java.util.List;
import org.jspecify.annotations.Nullable;

/**
 * A structural item (function, class, struct, etc.) in source code.
 *
 * <p>
 * {@code kindLabel} is the language-specific label carried by {@code StructureKind.Other} (e.g. {@code "resource"} for a Terraform resource
 * block, {@code "type"} for a GraphQL type, {@code "Macro"} for an Elixir macro). It is {@code null} for every unit variant, where
 * {@code kind()} alone is already exact.
 */
public record StructureItem(StructureKind kind, @Nullable String name, @Nullable String visibility, Span span,
        @Nullable List<StructureItem> children, @Nullable List<String> decorators, @Nullable String docComment, @Nullable String signature,
        @Nullable Span bodySpan, @Nullable String kindLabel) {
    /** Creates a new Builder for constructing instances of this record. */
    public static Builder builder() {
        return new Builder();
    }

    // CPD-OFF
    /** Jackson builder for StructureItem deserialization. */
    public static final class Builder {

        private StructureKind kind;

        @Nullable
        private String name;

        @Nullable
        private String visibility;

        private Span span;

        @Nullable
        private List<StructureItem> children;

        @Nullable
        private List<String> decorators;

        @Nullable
        private String docComment;

        @Nullable
        private String signature;

        @Nullable
        private Span bodySpan;

        @Nullable
        private String kindLabel;

        /** Sets the kind field. */
        public Builder withKind(final StructureKind value) {
            this.kind = value;
            return this;
        }

        /** Sets the name field. */
        public Builder withName(final @Nullable String value) {
            this.name = value;
            return this;
        }

        /** Sets the visibility field. */
        public Builder withVisibility(final @Nullable String value) {
            this.visibility = value;
            return this;
        }

        /** Sets the span field. */
        public Builder withSpan(final Span value) {
            this.span = value;
            return this;
        }

        /** Sets the children field. */
        public Builder withChildren(final @Nullable List<StructureItem> value) {
            this.children = value;
            return this;
        }

        /** Sets the decorators field. */
        public Builder withDecorators(final @Nullable List<String> value) {
            this.decorators = value;
            return this;
        }

        /** Sets the docComment field. */
        public Builder withDocComment(final @Nullable String value) {
            this.docComment = value;
            return this;
        }

        /** Sets the signature field. */
        public Builder withSignature(final @Nullable String value) {
            this.signature = value;
            return this;
        }

        /** Sets the bodySpan field. */
        public Builder withBodySpan(final @Nullable Span value) {
            this.bodySpan = value;
            return this;
        }

        /** Sets the kindLabel field — the payload of {@code StructureKind.Other}, null for unit variants. */
        public Builder withKindLabel(final @Nullable String value) {
            this.kindLabel = value;
            return this;
        }

        /** Constructs a StructureItem instance from the builder's current state. */
        public StructureItem build() {
            return new StructureItem(kind, name, visibility, span, children, decorators, docComment, signature, bodySpan, kindLabel);
        }
    }
    // CPD-ON
}
