{
  "id": "download_init_default",
  "description": "Initialize language pack with default config",
  "category": "download",
  "tags": ["download"],
  "call": "init_pack",
  "skip": {
    "languages": ["c", "wasm", "ruby"],
    "reason": "Download feature requires network access and is not available in static builds; ruby skipped because the Ruby binding does not expose an init function"
  },
  "input": {
    "config": {}
  },
  "assertions": [
    {
      "type": "not_error"
    }
  ]
}
